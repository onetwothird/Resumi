// Shared types for the frontend state
export interface ResumeData {
  id?: string;
  firstName: string;
  lastName: string;
  jobTitle: string;
  email: string;
  phone: string;
  address: string;
  summary: string;
}